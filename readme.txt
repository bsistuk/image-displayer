Set the file path in the first line of "config.txt" file.
In the next line, set the coordinates of top left point of the window.
Final line should have coordinates of the top left and bottom right point of the image in the window.
